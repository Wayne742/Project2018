# Wayne Reilly 16/04/2018.
# Programming and Scritping, End of Module Project.

import numpy as np #imports the python module known as numpy.

import matplotlib.pyplot as plt #imports the 

np.genfromtxt('data/iris.csv' , delimiter=',') #reads the iris.csv file into the script.
firstcol = data[:,0]#calls out the values in the first column of the data.
seccol = data[:,1]#calls out the values in the second column of the data.
thircol = data[:,2]#calls out the values in the third column of the data.
fourcol = data[:,3]#calls out the values in the fourth column of the data.
